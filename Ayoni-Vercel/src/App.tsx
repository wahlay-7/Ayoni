import { FormEvent, useEffect, useMemo, useState } from 'react';
import { ArrowRight, CheckCircle2, Edit3, ImagePlus, LogIn, LogOut, Package, Plus, RefreshCw, Search, ShieldCheck, ShoppingBag, Trash2, Upload, UserRound, X } from 'lucide-react';
import { hasSupabase, supabase } from './supabase';
import { seedProducts } from '../lib/products';

type Product={id:string;name:string;category:string;price:number;image:string;description:string;sizes:string[];colors:string[];stock:Record<string,number>;image_urls?:string[];created_at?:string};
type CartItem=Product & {quantity:number;size?:string;color?:string};
type Order={id:string;customer:any;items:any[];total:number;status:string;payment_status:string;paystack_reference?:string;created_at:string};
const money=(n:number)=>new Intl.NumberFormat('en-NG',{style:'currency',currency:'NGN',maximumFractionDigits:0}).format(n);
async function api(path:string,options:RequestInit={}){const session= supabase ? (await supabase.auth.getSession()).data.session : null;const headers=new Headers(options.headers);if(!(options.body instanceof FormData))headers.set('Content-Type','application/json');if(session?.access_token)headers.set('Authorization',`Bearer ${session.access_token}`);const r=await fetch(path,{...options,headers});const body=await r.json().catch(()=>({}));if(!r.ok)throw new Error(body.error||'Request failed');return body;}

function App(){
 const [products,setProducts]=useState<Product[]>([]),[category,setCategory]=useState('All'),[search,setSearch]=useState(''),[cart,setCart]=useState<CartItem[]>([]),[drawer,setDrawer]=useState(false),[checkout,setCheckout]=useState(false),[admin,setAdmin]=useState(false),[account,setAccount]=useState(false),[user,setUser]=useState<any>(null),[notice,setNotice]=useState('');
 const loadProducts=()=>api('/api/products').then(r=>setProducts(r.products?.length ? r.products : seedProducts.map((p,i)=>({...p,id:`demo-${i+1}`})))).catch(()=>setProducts(seedProducts.map((p,i)=>({...p,id:`demo-${i+1}`}))));
 useEffect(()=>{loadProducts();if(!supabase)return;supabase?.auth.getUser().then(({data})=>setUser(data.user));const {data}=supabase.auth.onAuthStateChange((_e,s)=>setUser(s?.user||null));return()=>data.subscription.unsubscribe()},[]);
 useEffect(()=>{const params=new URLSearchParams(location.search);const ref=params.get('reference');if(ref){api(`/api/paystack/verify?reference=${encodeURIComponent(ref)}`).then(r=>setNotice(r.paid?'Payment confirmed. Thank you!':'Payment could not be confirmed.')).catch(()=>setNotice('Payment verification failed.'));history.replaceState({},'',location.pathname)}},[]);
 const filtered=useMemo(()=>products.filter(p=>(category==='All'||p.category===category)&&p.name.toLowerCase().includes(search.toLowerCase())),[products,category,search]);
 const total=cart.reduce((s,x)=>s+x.price*x.quantity,0),count=cart.reduce((s,x)=>s+x.quantity,0);
 const add=(p:Product,size?:string,color?:string)=>{const key=variantKey(size,color);const stock=p.stock?.[key] ?? p.stock?.[size||'default'] ?? p.stock?.default;const current=cart.find(x=>x.id===p.id&&x.size===size&&x.color===color)?.quantity||0;if(stock!==undefined&&current>=stock)return setNotice('That variant is out of stock');setCart(c=>{const e=c.find(x=>x.id===p.id&&x.size===size&&x.color===color);return e?c.map(x=>x===e?{...x,quantity:x.quantity+1}:x):[...c,{...p,quantity:1,size,color}]});setNotice('Added to bag');setTimeout(()=>setNotice(''),1200)};
 const submit=async(e:FormEvent<HTMLFormElement>)=>{e.preventDefault();try{const f=Object.fromEntries(new FormData(e.currentTarget));const sh=await api(`/api/shipping?state=${encodeURIComponent(String(f.state))}`);const grand=total+Number(sh.fee||0);const r=await api('/api/orders',{method:'POST',body:JSON.stringify({customer:f,items:cart,total:grand})});setCart([]);setCheckout(false);setDrawer(false);if(r.paymentUrl)location.href=r.paymentUrl;else{const number=r.whatsappNumber||import.meta.env.VITE_AYONI_WHATSAPP_NUMBER||'';if(number)window.open(`https://wa.me/${number.replace(/\D/g,'')}?text=${encodeURIComponent(`Hello Ayoni, order ${r.orderId}. Total ${money(grand)}. Name: ${f.name}`)}`,'_blank');setNotice('Order received.')}}catch(err){setNotice(err instanceof Error?err.message:'Could not place order')}};
 if(admin)return <Admin products={products} onBack={()=>setAdmin(false)} onSaved={loadProducts}/>;
 if(account)return <Account onBack={()=>setAccount(false)} user={user}/>;
 return <div className="site"><header className="header"><div className="logo">AYONI</div><nav>{['All','Clothing','Shoes','Eyewear'].map(x=><button className={category===x?'active':''} key={x} onClick={()=>setCategory(x)}>{x}</button>)}</nav><div className="head-actions"><label className="search"><Search size={17}/><input value={search} onChange={e=>setSearch(e.target.value)} placeholder="Search"/></label><button className="icon-btn" onClick={async()=>{if(user){if(supabase)await supabase.auth.signOut();setUser(null)}else{if(!hasSupabase){setNotice('Customer accounts are enabled after Supabase is connected.');return}const email=prompt('Email');const password=prompt('Password');if(email&&password){const r=await supabase!.auth.signInWithPassword({email,password});if(r.error)setNotice(r.error.message);else setUser(r.data.user)}}}}>{user?<LogOut/>:<UserRound/>}</button><button className="bag" onClick={()=>setDrawer(true)}><ShoppingBag/><span>{count}</span></button></div></header>
 <section className="hero"><div><p className="eyebrow">ILORIN · KWARA</p><h1>Wear your<br/><em>own rhythm.</em></h1><p className="hero-copy">Modern essentials for people who move differently. Unisex clothing, footwear and eyewear, curated by Ayoni.</p><button className="primary" onClick={()=>document.getElementById('shop')?.scrollIntoView({behavior:'smooth'})}>Shop collection <ArrowRight/></button></div><div className="hero-art"><div className="hero-card"><span>AYONI / 01</span><strong>EVERYDAY<br/>FORM</strong></div></div></section>
 <section id="shop" className="shop"><div className="section-head"><div><p className="eyebrow">THE COLLECTION</p><h2>Selected pieces</h2></div><span>{filtered.length} pieces</span></div><div className="grid">{filtered.map(p=><ProductCard key={p.id} p={p} onAdd={add}/>)}</div>{!filtered.length&&<div className="empty-state">No products match your search.</div>}</section>
 <section className="manifesto"><p className="eyebrow">WHY AYONI</p><h2>Simple forms.<br/><em>Strong presence.</em></h2><div className="manifesto-grid"><p>Designed around versatility, Ayoni brings considered silhouettes and everyday accessories into one unisex wardrobe.</p><div><ShieldCheck/><span>Secure checkout</span><Package/><span>State-based delivery</span></div></div></section>
 <footer><div className="logo">AYONI</div><p>Modern essentials · Ilorin, Kwara, Nigeria</p><button onClick={()=>setAdmin(true)}>Admin</button><button onClick={()=>setAccount(true)}>My orders</button></footer>
 {drawer&&<div className="overlay" onClick={()=>setDrawer(false)}><aside className="drawer" onClick={e=>e.stopPropagation()}><div className="drawer-head"><h2>Your bag</h2><button className="icon-btn" onClick={()=>setDrawer(false)}><X/></button></div>{cart.length?cart.map((x,i)=><div className="cart-item" key={i}><img src={x.image}/><div><h3>{x.name}</h3><span>{money(x.price)} · Qty {x.quantity}</span><p>{[x.size,x.color].filter(Boolean).join(' · ')}</p></div></div>):<p className="empty">Your bag is empty.</p>}<div className="cart-total"><span>Total</span><strong>{money(total)}</strong></div>{cart.length>0&&<button className="primary wide" onClick={()=>setCheckout(true)}>Checkout</button>}</aside></div>}
 {checkout&&<div className="overlay"><form className="checkout" onSubmit={submit}><div className="drawer-head"><h2>Checkout</h2><button type="button" className="icon-btn" onClick={()=>setCheckout(false)}><X/></button></div><p className="checkout-note">Delivery is calculated by destination state.</p><input name="name" required placeholder="Full name"/><input name="phone" required placeholder="Phone number"/><input name="email" type="email" required defaultValue={user?.email||''} placeholder="Email address"/><input name="address" required placeholder="Delivery address"/><select name="state" defaultValue="Kwara"><option>Kwara</option><option>Lagos</option><option>Abuja</option><option>Other</option></select><button className="primary wide">Continue · {money(total)}</button></form></div>}
 {notice&&<div className="toast" onClick={()=>setNotice('')}>{notice}</div>}</div>;
}
function variantKey(size?:string,color?:string){return `${size||'default'}::${color||'default'}`}
function ProductCard({p,onAdd}:{p:Product;onAdd:(p:Product,s?:string,c?:string)=>void}){const [size,setSize]=useState(p.sizes?.[0]||''),[color,setColor]=useState(p.colors?.[0]||'');const image=p.image||p.image_urls?.[0]||'';return <article className="product"><div className="product-image">{image?<img src={image} alt={p.name}/>:<div className="image-placeholder">AYONI</div>}<button onClick={()=>onAdd(p,size,color)}><Plus/></button></div><div className="product-info"><div><span>{p.category}</span><h3>{p.name}</h3>{p.sizes?.length>0&&<select value={size} onChange={e=>setSize(e.target.value)}>{p.sizes.map(s=><option key={s}>{s}</option>)}</select>}{p.colors?.length>0&&<select value={color} onChange={e=>setColor(e.target.value)}>{p.colors.map(c=><option key={c}>{c}</option>)}</select>}</div><strong>{money(Number(p.price))}</strong></div></article>}
function Account({onBack,user}:{onBack:()=>void;user:any}){const [d,setD]=useState<any>();useEffect(()=>{if(user)api('/api/account').then(setD).catch(e=>setD({error:e.message}))},[user]);return <div className="admin"><header className="admin-head"><div><p className="eyebrow">AYONI ACCOUNT</p><h1>My orders</h1></div><button className="secondary" onClick={onBack}>Back</button></header><div className="panel">{user?(d?.orders?.length?d.orders.map((o:any)=><div className="order-row" key={o.id}><div><b>#{o.id.slice(0,8)}</b><span>{String(o.customer?.name||'Customer')} · {money(Number(o.total))}</span></div><span>{o.status}</span></div>):<p>No orders yet.</p>):<p>Sign in to see your orders.</p>}</div></div>}

function Admin({products,onBack,onSaved}:{products:Product[];onBack:()=>void;onSaved:()=>void}){const [sessionUser,setSessionUser]=useState<any>(null),[checking,setChecking]=useState(true),[email,setEmail]=useState(''),[password,setPassword]=useState(''),[loginMsg,setLoginMsg]=useState(''),[tab,setTab]=useState<'products'|'orders'>('products');useEffect(()=>{supabase?.auth.getUser().then(({data})=>{setSessionUser(data.user);setChecking(false)})},[]);if(checking)return <div className="admin loading">Checking admin access…</div>;if(!sessionUser)return <AdminLogin email={email} setEmail={setEmail} password={password} setPassword={setPassword} msg={loginMsg} setMsg={setLoginMsg} onBack={onBack} onSuccess={u=>setSessionUser(u)}/>;return <AdminPanel products={products} onBack={onBack} onSaved={onSaved} tab={tab} setTab={setTab} user={sessionUser}/>}
function AdminLogin({email,setEmail,password,setPassword,msg,setMsg,onBack,onSuccess}:{email:string;setEmail:(v:string)=>void;password:string;setPassword:(v:string)=>void;msg:string;setMsg:(v:string)=>void;onBack:()=>void;onSuccess:(u:any)=>void}){const submit=async(e:FormEvent)=>{e.preventDefault();setMsg('Signing in…');const r=await supabase!.auth.signInWithPassword({email,password});if(r.error){setMsg(r.error.message);return}onSuccess(r.data.user)};return <div className="admin"><div className="admin-login"><div className="logo">AYONI</div><p className="eyebrow">PRIVATE ADMIN</p><h1>Store control room</h1><p>Sign in with the Supabase account whose email matches <b>AYONI_ADMIN_EMAIL</b> in Vercel.</p><form onSubmit={submit}><input type="email" value={email} onChange={e=>setEmail(e.target.value)} required placeholder="Admin email"/><input type="password" value={password} onChange={e=>setPassword(e.target.value)} required placeholder="Password"/><button className="primary wide"><LogIn/> Sign in</button></form>{msg&&<p className="form-msg">{msg}</p>}<button className="secondary wide" onClick={onBack}>Back to store</button></div></div>}

function AdminPanel({products,onBack,onSaved,tab,setTab,user}:{products:Product[];onBack:()=>void;onSaved:()=>void;tab:'products'|'orders';setTab:(v:'products'|'orders')=>void;user:any}){const [editing,setEditing]=useState<Product|null>(null),[orders,setOrders]=useState<Order[]>([]),[msg,setMsg]=useState(''),[loading,setLoading]=useState(false);const loadOrders=async()=>{setLoading(true);try{const r=await api('/api/orders');setOrders(r.orders||[])}catch(e){setMsg(e instanceof Error?e.message:'Could not load orders')}finally{setLoading(false)}};useEffect(()=>{if(tab==='orders')loadOrders()},[tab]);const saveProduct=async(p:Partial<Product>,file?:File)=>{setLoading(true);try{let image=p.image||'';if(file){const content=await fileToDataUrl(file);const up=await api('/api/products/upload',{method:'POST',body:JSON.stringify({content,contentType:file.type,name:file.name})});image=up.url}const payload={...p,image,imageUrls:p.image_urls||[]};const r=editing?await api(`/api/products/${editing.id}`,{method:'PUT',body:JSON.stringify(payload)}):await api('/api/products',{method:'POST',body:JSON.stringify(payload)});setMsg(editing?'Product updated.':'Product published.');setEditing(null);onSaved();if(r.product){} }catch(e){setMsg(e instanceof Error?e.message:'Could not save product')}finally{setLoading(false)}};const deleteProduct=async(p:Product)=>{if(!confirm(`Delete “${p.name}”?`))return;setLoading(true);try{await api(`/api/products/${p.id}`,{method:'DELETE'});setMsg('Product deleted.');onSaved()}catch(e){setMsg(e instanceof Error?e.message:'Could not delete product')}finally{setLoading(false)}};return <div className="admin"><header className="admin-head"><div><p className="eyebrow">AYONI MANAGEMENT</p><h1>Store control room</h1><span>{user.email}</span></div><div className="admin-actions"><button className={tab==='products'?'tab active':'tab'} onClick={()=>setTab('products')}>Products</button><button className={tab==='orders'?'tab active':'tab'} onClick={()=>setTab('orders')}>Orders</button><button className="secondary" onClick={async()=>{if(supabase)await supabase.auth.signOut();onBack()}}>Sign out</button><button className="secondary" onClick={onBack}>Store</button></div></header>{msg&&<div className="admin-message">{msg}<button onClick={()=>setMsg('')}><X size={16}/></button></div>}{tab==='products'?<div className="admin-products"><ProductEditor product={editing} loading={loading} onCancel={()=>setEditing(null)} onSave={saveProduct}/><section className="panel"><div className="panel-head"><div><p className="eyebrow">CATALOG</p><h2>{products.length} products</h2></div><button className="secondary" onClick={onSaved}><RefreshCw size={16}/> Refresh</button></div><div className="admin-list">{products.map(p=><div className="admin-product" key={p.id}><div className="admin-thumb">{p.image?<img src={p.image} alt=""/>:<ImagePlus/>}</div><div className="admin-product-main"><b>{p.name}</b><span>{p.category} · {money(Number(p.price))}</span><small>{(p.sizes||[]).length} sizes · {(p.colors||[]).length} colors · {stockTotal(p.stock)} units</small></div><button className="icon-btn" onClick={()=>setEditing(p)} title="Edit"><Edit3/></button><button className="icon-btn danger" onClick={()=>deleteProduct(p)} title="Delete"><Trash2/></button></div>)}{!products.length&&<p>No products yet. Add your first product.</p>}</div></section></div>:<OrderManager orders={orders} loading={loading} onRefresh={loadOrders} onMessage={setMsg}/>}</div>}

function ProductEditor({product,loading,onCancel,onSave}:{product:Product|null;loading:boolean;onCancel:()=>void;onSave:(p:Partial<Product>,file?:File)=>Promise<void>}){const [name,setName]=useState(''),[category,setCategory]=useState('Clothing'),[price,setPrice]=useState(''),[description,setDescription]=useState(''),[sizes,setSizes]=useState<string[]>([]),[colors,setColors]=useState<string[]>([]),[stock,setStock]=useState<Record<string,number>>({}),[image,setImage]=useState(''),[file,setFile]=useState<File>();useEffect(()=>{setName(product?.name||'');setCategory(product?.category||'Clothing');setPrice(product?String(product.price):'');setDescription(product?.description||'');setSizes(product?.sizes||[]);setColors(product?.colors||[]);setStock(product?.stock||{});setImage(product?.image||'');setFile(undefined)},[product]);const addToken=(value:string,setter:(v:string[])=>void,current:string[])=>{const v=value.trim();if(v&&!current.includes(v))setter([...current,v])};const variantList=(sizes.length?sizes:['default']).flatMap(s=>(colors.length?colors:['default']).map(c=>({s,c,key:variantKey(s,c)})));const total=stockTotal(stock);const submit=(e:FormEvent)=>{e.preventDefault();onSave({name,category,price:Number(price),description,sizes,colors,stock,image});};return <section className="panel editor"><div className="panel-head"><div><p className="eyebrow">{product?'EDIT PRODUCT':'NEW PRODUCT'}</p><h2>{product?product.name:'Add product'}</h2></div>{product&&<button className="icon-btn" onClick={onCancel}><X/></button>}</div><form onSubmit={submit} className="editor-form"><label className="image-upload"><div className="preview">{file?<img src={URL.createObjectURL(file)} alt="Preview"/>:image?<img src={image} alt="Current"/>:<><ImagePlus size={32}/><span>Upload product photo</span></>}</div><div><b><Upload size={16}/> Choose photo</b><small>JPG, PNG or WebP · max 5MB</small></div><input type="file" accept="image/jpeg,image/png,image/webp" onChange={e=>setFile(e.target.files?.[0])}/></label><input value={name} onChange={e=>setName(e.target.value)} required placeholder="Product name"/><div className="two"><select value={category} onChange={e=>setCategory(e.target.value)}><option>Clothing</option><option>Shoes</option><option>Eyewear</option></select><input value={price} onChange={e=>setPrice(e.target.value)} type="number" min="0" required placeholder="Price (NGN)"/></div><TagInput label="Sizes" values={sizes} setValues={setSizes} placeholder="Add size e.g. M"/><TagInput label="Colors" values={colors} setValues={setColors} placeholder="Add color e.g. Black"/><div><div className="field-label">Stock by variant <span>{total} units</span></div>{variantList.map(v=><label className="stock-row" key={v.key}><span>{v.s} · {v.c}</span><input type="number" min="0" value={stock[v.key]??0} onChange={e=>setStock({...stock,[v.key]:Math.max(0,Number(e.target.value))})}/></label>)}</div><textarea value={description} onChange={e=>setDescription(e.target.value)} placeholder="Product description"/><div className="editor-buttons">{product&&<button type="button" className="secondary" onClick={onCancel}>Cancel</button>}<button disabled={loading} className="primary wide">{loading?'Saving…':product?'Save changes':'Publish product'}</button></div></form></section>}
function TagInput({label,values,setValues,placeholder}:{label:string;values:string[];setValues:(v:string[])=>void;placeholder:string}){const [value,setValue]=useState('');return <div><div className="field-label">{label}</div><div className="tags">{values.map(v=><span key={v}>{v}<button type="button" onClick={()=>setValues(values.filter(x=>x!==v))}>×</button></span>)}</div><div className="tag-add"><input value={value} onChange={e=>setValue(e.target.value)} onKeyDown={e=>{if(e.key==='Enter'){e.preventDefault();add()}}} placeholder={placeholder}/><button type="button" className="secondary" onClick={add}><Plus size={15}/> Add</button></div></div>;function add(){if(value.trim()&&!values.includes(value.trim())){setValues([...values,value.trim()]);setValue('')}}}

function OrderManager({orders,loading,onRefresh,onMessage}:{orders:Order[];loading:boolean;onRefresh:()=>Promise<void>;onMessage:(v:string)=>void}){const update=async(id:string,status:string)=>{try{await api(`/api/orders/${id}`,{method:'PUT',body:JSON.stringify({status})});onMessage('Order status updated.');await onRefresh()}catch(e){onMessage(e instanceof Error?e.message:'Update failed')}};return <section className="panel orders-panel"><div className="panel-head"><div><p className="eyebrow">FULFILMENT</p><h2>{orders.length} orders</h2></div><button className="secondary" onClick={onRefresh} disabled={loading}><RefreshCw size={16}/> Refresh</button></div>{orders.map(o=><div className="order-card" key={o.id}><div><b>#{o.id.slice(0,8)}</b><span>{o.customer?.name||'Customer'} · {o.customer?.phone||''}</span><small>{new Date(o.created_at).toLocaleString('en-NG')} · {money(Number(o.total))} · payment: {o.payment_status}</small><small>{Array.isArray(o.items)?o.items.map((i:any)=>`${i.name} × ${i.quantity}${i.size?` / ${i.size}`:''}${i.color?` / ${i.color}`:''}`).join(' · '):''}</small></div><select value={o.status} onChange={e=>update(o.id,e.target.value)}>{['pending','paid','processing','shipped','delivered','cancelled'].map(s=><option key={s}>{s}</option>)}</select></div>)}{!orders.length&&<div className="empty-state">No orders yet.</div>}</section>}
function stockTotal(stock:Record<string,number>={}){return Object.values(stock||{}).reduce((a,b)=>a+Number(b||0),0)}
function fileToDataUrl(file:File){return new Promise<string>((resolve,reject)=>{if(file.size>5*1024*1024)return reject(new Error('Image must be 5MB or smaller.'));const r=new FileReader();r.onload=()=>resolve(String(r.result));r.onerror=reject;r.readAsDataURL(file)})}
export default App;
